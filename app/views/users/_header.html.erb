<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<% if user_signed_in? %>
  <div class="container-fluid">
  <%= link_to 'Recipe App', user_path(current_user), class: 'navbar-brand'%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Foods', user_foods_path(current_user) ,class: 'nav-link odd' %>
        </li>
        <li class='nav-link'><%= link_to 'Recipes', user_recipes_path(current_user), class: 'nav-link odd'%></li>
         <li class='nav-link'><%= link_to 'Public Recipes', user_public_recipes_path(current_user), class: 'nav-link odd'%></li>
      </ul>
      
    </div>
    <li class="d-flex justify-content-end ml-auto">
        <% if user_signed_in? %>
        <%= button_to 'Sign Out', destroy_user_session_path(return_to: new_user_session_path), method: :delete, class: 'btn btn-warning  text-end' %>
        <% else %>
        <%= button_to 'Sign In', new_user_session_path, class: 'nav-link btn btn-warning text-end'%>
        <% end%>
        </li>
   </div>
   <%end%>
</nav>