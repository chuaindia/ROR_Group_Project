<p style="color: cyan"><%= notice %></p>

<%= render @food %>

<div class="container bg-warning rounded-top">
  <div class="d-flex">
    <div>
      <%= link_to "Back to foods", user_foods_path(@food), class: "btn btn-secondary m-2 mb-10 text-decoration-none" %>
    </div>
    <div class="ms-auto m-1 order-last">
      <h3><%= button_to "Delete", user_food_path, method: :delete,  class: 'btn btn-secondary', data: { turbo: false }  %></h3>
    </div>
  </div>
</div>